<persistence xmlns="https://jakarta.ee/xml/ns/persistence" version="3.0">
    
    <persistence-unit name="CompendioPU" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>


        <class>it.unicam.cs.mpgc.rpg125730.persistence.entity.DiscoveredShadowEntity</class>

        <properties>
            <!-- Connessione a un database SQLite locale (un singolo file chiamato compendio.db) -->
            <property name="jakarta.persistence.jdbc.driver" value="org.sqlite.JDBC"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:sqlite:compendio.db"/>


            <property name="hibernate.dialect" value="org.hibernate.community.dialect.SQLiteDialect"/>

            <!-- Creazione automatica delle tabelle al primo avvio -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.show_sql" value="false"/>
        </properties>
    </persistence-unit>
</persistence>